type: http
scheme: bearer
bearerFormat: JWT
description: >-
  OCM JWT bearer token. The token is validated against the
  Red Hat SSO JWKS endpoint; caller identity is extracted
  from the username JWT claim.
